import styled from "styled-components";

export const ResetPassForm = styled.div`
  .formSection {
    margin: 0 auto;
    padding: 1rem;
  }
  .formSection > p {
    color: #ffffff;
    font-size: 0.75rem;
  }
`;
